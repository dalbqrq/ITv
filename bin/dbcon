#!/bin/bash 

progdir=$(dirname $(readlink -f $0))
. $progdir/dbconf

if [ $# -eq 1 ]; then
	echo "$1" | mysql -u $dbuser --password=$dbpass $dbname
else
	mysql -u $dbuser --password=$dbpass $dbname
fi

